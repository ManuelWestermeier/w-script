#include <string>
#include <iostream>

using namespace std;

#define str string
#define txt string
#define num float
#define sf to_string

void out(str data) { cout << data << endl; }
void print(str data) { cout << data << endl; }
void log(str data) { cout << data << endl; }

// Manuel Westermeier
